const LINE = "----------------------------------------------------------------------";

const RELEASE_TYPES = ["prerelease", "prepatch", "patch", "preminor", "minor", "premajor", "major"];

module.exports = {
  LINE,
  RELEASE_TYPES,
};
