const fs = require("fs");
const path = require("path");

export function readFolder(folderPath) {
  const fileList = [];

  fs.

  return {
    files: fileList,
    folderPath
  }
}

export function writeFiles(folderPath) {}
