import {Paper} from "@mantine/core";
import React from "react";

/**
 * @typedef PanelTreeProps
 * @property {string} [className]
 */

/** @type {React.FC<PanelTreeProps>} */
export const PanelTree = props => {
  const {} = props;

  return (
    <Paper shadow="xs">

    </Paper>
  );
};
